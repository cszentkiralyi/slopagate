//const { replaceTextInFile } = require('./replace.js');
import { replaceTextInFile } from './replace.js';

// TODO: read ARGV[2] for subtool
// TODO: based on subtool, call corresponding function with rest of ARGV[] as array arg

replaceTextInFile();