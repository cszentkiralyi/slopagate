# Setup Notes

- Initialize git repo with `git init` and commit initial files
- Run `npm install` to install dependencies
- Configure `~/.slopagate/config.json` for model endpoint
- Start Ollama server with `ollama serve`
- Open Slopagate with `node src/slopagate.js`
